apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: redmine-hpa
  namespace: pmotrack
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: redmine
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50

